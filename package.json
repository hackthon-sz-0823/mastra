{
  "name": "waste-classification",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "mastra dev",
    "build": "mastra build",
    "start": "mastra start",
    "build:worker": "npx tsc src/worker.ts --outDir dist --target ES2022 --module ESNext --moduleResolution node --skipLibCheck",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env development",
    "wrangler:dev": "wrangler dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.3.23",
    "@mastra/core": "^0.12.1",
    "@mastra/libsql": "^0.12.0",
    "@mastra/loggers": "^0.10.5",
    "@mastra/memory": "^0.12.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "mastra": "^0.10.18",
    "typescript": "^5.9.2",
    "wrangler": "^4.32.0"
  }
}
